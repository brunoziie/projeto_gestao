<h1>SPRINT #<%= @sprint.number %></h1>

<p><%= @sprint.status_humanize %> <%= "(Atrasada)" if @sprint.late?%></p>

<% if @sprint.late? %>
  <p>Atrasada</p>
<% end %>

<% if @sprint.scheduled? and policy(Project).edit? %>
  <p><%= link_to 'Iniciar Sprint', root_path %></p>
<% end %>

<% if @sprint.doing? and policy(Project).edit? %>
  <p><%= link_to 'Finalizar Sprint', root_path %></p>
<% end %>

<% if policy(Project).edit? %>
  <p><%= link_to 'Apagar Sprint', project_sprint_path(@project, @sprint), method: :delete, data: { confirm: "Você tem Certeza?"} %></p>
<% end %>

